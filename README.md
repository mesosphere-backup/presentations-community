presentations
=============

Slide decks from presentations given by/with Mesos community partners.

Adding a new presentation? Remember to update the top-level `index.html` using `./make_index.sh > index.html`.
